---
import AnchorHeading from "@astrojs/starlight/components/AnchorHeading.astro";

const {title, level} = Astro.props;
const id = title.toLowerCase();
---

<AnchorHeading level={level} id={id}>{title}<slot /></AnchorHeading>