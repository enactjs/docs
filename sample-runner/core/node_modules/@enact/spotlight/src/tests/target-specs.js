"use strict";

var _container10 = require("../container");
var _target = require("../target");
var _utils = require("./utils");
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
var nonSpottable = function nonSpottable() {
  return (0, _utils.node)({
    className: 'other'
  });
};
var position = function position(top, left) {
  return "position: absolute; top: ".concat(top, "px; left: ").concat(left, "px; height: 10px; width: 10px;");
};
var positionedSpottable = function positionedSpottable(id, top, left) {
  return (0, _utils.spottable)({
    id: id,
    style: position(top, left)
  });
};
var grid = function grid() {
  return (0, _utils.container)(_defineProperty(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'grid'), "style", 'position: relative; height: 30px; width: 30px;'), "children", (0, _utils.join)(positionedSpottable('top-left', 0, 0), positionedSpottable('top-center', 0, 10), positionedSpottable('top-right', 0, 20), positionedSpottable('middle-left', 10, 0), positionedSpottable('middle-center', 10, 10), positionedSpottable('middle-right', 10, 20), positionedSpottable('bottom-left', 20, 0), positionedSpottable('bottom-center', 20, 10), positionedSpottable('bottom-right', 20, 20))));
};
var scenarios = {
  complexTree: (0, _utils.join)((0, _utils.spottable)(nonSpottable()), (0, _utils.container)(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'first-container'), "children", (0, _utils.join)((0, _utils.someSpottables)(2), (0, _utils.container)(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'second-container'), "children", (0, _utils.join)((0, _utils.someSpottables)(3), (0, _utils.container)(_defineProperty(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'third-container'), 'data-spotlight-container-disabled', true), "children", (0, _utils.join)((0, _utils.someSpottables)(4), (0, _utils.node)({
    id: 'child-of-third'
  }))))))))))),
  nonSpottableInContainer: (0, _utils.join)((0, _utils.spottable)({
    id: 'in-root'
  }), (0, _utils.container)(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'first'), "children", (0, _utils.join)(nonSpottable(), (0, _utils.spottable)({
    id: 'in-first'
  })))), (0, _utils.container)(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'second'), "children", (0, _utils.join)(nonSpottable())))),
  grid: (0, _utils.join)((0, _utils.spottable)({
    id: 'before-grid',
    style: 'height: 10px'
  }), grid(), (0, _utils.spottable)({
    id: 'after-grid',
    style: 'height: 10px'
  })),
  overlap: (0, _utils.node)({
    style: 'position: relative',
    children: (0, _utils.join)(grid(), (0, _utils.spottable)({
      id: 'over-middle-center',
      style: 'position: absolute; top: 12px; left: 15px; height: 1px; width: 1px;'
    }))
  }),
  overflow: (0, _utils.join)((0, _utils.spottable)({
    id: 'outside-overflow'
  }), (0, _utils.node)({
    // allocate some empty space so that overflow items would be otherwise navigable
    style: 'height: 100px'
  }), (0, _utils.container)(_defineProperty(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'overflow-container'), "style", 'position: relative; height: 30px; width: 30px;'), "children", (0, _utils.join)((0, _utils.spottable)({
    id: 'overflow-above',
    style: 'position: absolute; top: -10px; left: 0px; height: 10px; width: 10px;'
  }), (0, _utils.spottable)({
    id: 'overflow-below',
    style: 'position: absolute; top: 30px; left: 0px; height: 10px; width: 10px;'
  }), (0, _utils.spottable)({
    id: 'overflow-within',
    style: 'position: absolute; top: 0px; left: 0px; height: 10px; width: 10px;'
  }))))),
  overflowLargeSubContainer: (0, _utils.join)((0, _utils.spottable)({
    id: 'outside-overflow'
  }), (0, _utils.node)({
    // allocate some empty space so that overflow items would be otherwise navigable
    style: 'height: 100px'
  }), (0, _utils.container)(_defineProperty(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'overflow-container'), "style", 'position: relative; height: 30px; width: 30px;'), "children", (0, _utils.join)((0, _utils.container)(_defineProperty(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'inside'), "style", 'position: absolute; top: -10px; left: 0px; height: 50px; width: 10px;'), "children", (0, _utils.join)((0, _utils.node)({
    // allocate space to push following spottable into view
    style: 'height: 10px'
  }), (0, _utils.spottable)({
    id: 'in-large-container'
  })))), (0, _utils.spottable)({
    id: 'below-large-container',
    style: 'position: absolute; top: 40px; left: 0px; height: 10px; width: 10px;'
  }))))),
  emptyContainer: (0, _utils.join)(positionedSpottable('above', 0, 10), (0, _utils.container)(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'empty-container'), "style", position(10, 10))), positionedSpottable('below', 30, 0)),
  emptyContainerOverlap: (0, _utils.join)(positionedSpottable('above', 5, 10), (0, _utils.container)(_defineProperty(_defineProperty({}, _container10.containerAttribute, 'empty-container'), "style", position(10, 10))), positionedSpottable('below', 30, 0))
};
var safeTarget = function safeTarget(n, fn) {
  return n ? fn(n) : 'NOT FOUND';
};
var setupContainers = function setupContainers() {
  (0, _container10.configureDefaults)({
    selector: '.spottable'
  });
  (0, _container10.configureContainer)(_container10.rootContainerId);
};
var teardownContainers = function teardownContainers() {
  // clean up any containers we create for safe tests
  (0, _container10.getAllContainerIds)().forEach(_container10.removeContainer);
  (0, _container10.setDefaultContainer)();
};

// NOTE: Skipping most tests because JSDOM does not support measurements
describe('target', function () {
  beforeEach(setupContainers);
  afterEach(teardownContainers);
  describe('#getNavigableTarget', function () {
    test('should find spottable parent', (0, _utils.testScenario)(scenarios.complexTree, function (root) {
      var other = root.querySelector('.other');
      var expected = other.parentNode;
      var actual = (0, _target.getNavigableTarget)(other);
      expect(actual).toBe(expected);
    }));
    test('should skip containers', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function (root) {
      (0, _container10.configureContainer)('first');
      var other = root.querySelector('.other');
      var expected = null;
      var actual = (0, _target.getNavigableTarget)(other);
      expect(actual).toBe(expected);
    }));
    test('should respect container-specific selector', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function (root) {
      // make '.other' a valid spottable element
      (0, _container10.configureContainer)('first', {
        selector: '.other'
      });
      var other = root.querySelector('.other');
      var expected = other;
      var actual = (0, _target.getNavigableTarget)(other);
      expect(actual).toBe(expected);
    }));
    test('should respect disabled containers', (0, _utils.testScenario)(scenarios.complexTree, function (root) {
      (0, _container10.configureContainer)('third-container');
      var other = root.querySelector("[".concat(_container10.containerAttribute, "='third-container'] .spottable"));
      var expected = null;
      var actual = (0, _target.getNavigableTarget)(other);
      expect(actual).toBe(expected);
    }));
  });
  describe('#getTargetByContainer', function () {
    test('should find spottable element within provided container', (0, _utils.testScenario)(scenarios.complexTree, function () {
      (0, _container10.configureContainer)('first-container');
      var expected = 'spottable';
      var actual = safeTarget((0, _target.getTargetByContainer)('first-container'), function (t) {
        return t.className;
      });
      expect(actual).toBe(expected);
    }));
    test('should return null when container does not contain any spottable elements', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first');
      (0, _container10.configureContainer)('second');
      var expected = null;
      var actual = (0, _target.getTargetByContainer)('second');
      expect(actual).toBe(expected);
    }));
    test('should find the first spottable in the root when no container specified', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first');
      (0, _container10.configureContainer)('second');
      var expected = 'in-root';
      var actual = safeTarget((0, _target.getTargetByContainer)(), function (t) {
        return t.id;
      });
      expect(actual).toBe(expected);
    }));
    test('should find the first spottable in the default container when set and no container specified', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first');
      (0, _container10.configureContainer)('second');
      (0, _container10.setDefaultContainer)('first');
      var expected = 'spottable';
      var actual = safeTarget((0, _target.getTargetByContainer)(), function (t) {
        return t.className;
      });
      expect(actual).toBe(expected);
    }));
    test('should return `default-element` when last focused is unset', (0, _utils.testScenario)(scenarios.grid, function () {
      (0, _container10.configureContainer)('grid', {
        enterTo: 'last-focused',
        defaultElement: '#middle-center'
      });
      var expected = 'middle-center';
      var actual = safeTarget((0, _target.getTargetByContainer)('grid'), function (t) {
        return t.id;
      });
      expect(actual).toBe(expected);
    }));
    test('should return `default-element` when `last-focused` is requested but is unset', (0, _utils.testScenario)(scenarios.grid, function () {
      (0, _container10.configureContainer)('grid', {
        defaultElement: '#middle-center'
      });
      var expected = 'middle-center';
      var actual = safeTarget((0, _target.getTargetByContainer)('grid', 'last-focused'), function (t) {
        return t.id;
      });
      expect(actual).toBe(expected);
    }));
    test('should return default element when configured for `last-focused` but requested `default-element`', (0, _utils.testScenario)(scenarios.grid, function () {
      (0, _container10.configureContainer)('grid', {
        enterTo: 'last-focused',
        lastFocusedElement: document.querySelector('#top-left'),
        defaultElement: '#middle-center'
      });
      var expected = 'middle-center';
      var actual = safeTarget((0, _target.getTargetByContainer)('grid', 'default-element'), function (t) {
        return t.id;
      });
      expect(actual).toBe(expected);
    }));
    test('should return last focused element when configured for `default-element` but requested `last-focused`', (0, _utils.testScenario)(scenarios.grid, function () {
      (0, _container10.configureContainer)('grid', {
        enterTo: 'default-element',
        lastFocusedElement: document.querySelector('#top-left'),
        defaultElement: '#middle-center'
      });
      var expected = 'top-left';
      var actual = safeTarget((0, _target.getTargetByContainer)('grid', 'last-focused'), function (t) {
        return t.id;
      });
      expect(actual).toBe(expected);
    }));
  });
  describe('#getTargetBySelector', function () {
    test('should find spottable element within container when "@" prefix used', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first');
      var expected = 'spottable';
      var actual = safeTarget((0, _target.getTargetBySelector)('@first'), function (t) {
        return t.className;
      });
      expect(actual).toBe(expected);
    }));
    test('should find spottable element within container when "#" prefix used', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first');
      var expected = 'in-first';
      var actual = safeTarget((0, _target.getTargetBySelector)('#in-first'), function (t) {
        return t.id;
      });
      expect(actual).toBe(expected);
    }));
    test('should return null when the node exists but is not navigable within its container', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first', {
        navigableFilter: function navigableFilter() {
          return false;
        }
      });
      var expected = null;
      var actual = (0, _target.getTargetBySelector)('#in-first');
      expect(actual).toBe(expected);
    }));
    test('should return null when the node exists but does not match the container\'s selector', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      (0, _container10.configureContainer)('first');
      var expected = null;
      var actual = (0, _target.getTargetBySelector)("[".concat(_container10.containerAttribute, "='first'] .other"));
      expect(actual).toBe(expected);
    }));
    test('should return null for an empty selectors', (0, _utils.testScenario)(scenarios.nonSpottableInContainer, function () {
      var expected = null;

      // eslint-disable-next-line no-undefined
      expect((0, _target.getTargetBySelector)(undefined)).toBe(expected);
      expect((0, _target.getTargetBySelector)(null)).toBe(expected);
      expect((0, _target.getTargetBySelector)('')).toBe(expected);
    }));
  });
  describe('#getTargetByDirectionFromElement', function () {
    test('should find target within container by direction', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid');
      var center = root.querySelector('#middle-center');
      var topCenter = root.querySelector('#top-center');
      var bottomCenter = root.querySelector('#bottom-center');
      var middleLeft = root.querySelector('#middle-left');
      var middleRight = root.querySelector('#middle-right');
      center.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 0,
          width: 10,
          height: 10
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('up', center), function (t) {
        return t.id;
      })).toBe('top-center');
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', center), function (t) {
        return t.id;
      })).toBe('bottom-center');
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('left', center), function (t) {
        return t.id;
      })).toBe('middle-left');
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('right', center), function (t) {
        return t.id;
      })).toBe('middle-right');
    }));
    test('should find target within container from floating element', (0, _utils.testScenario)(scenarios.overlap, function (root) {
      (0, _container10.configureContainer)('grid', {
        enterTo: 'default-element',
        defaultElement: '#middle-center'
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var overlap = root.querySelector('#over-middle-center');
      var center = root.querySelector('#middle-center');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock getBoundingClientRect for all elements
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      center.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };

      // Overlap is a small element floating over middle-center
      overlap.getBoundingClientRect = function () {
        return {
          top: 22,
          left: 25,
          width: 1,
          height: 1,
          right: 26,
          bottom: 23
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', overlap), function (t) {
        return t.id;
      })).toBe('middle-center');
    }));
    test('should ignore targets outside the bounds of an overflow container', (0, _utils.testScenario)(scenarios.overflow, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      var overflowContainer = root.querySelector('[data-spotlight-id="overflow-container"]');
      var element = root.querySelector('#outside-overflow');
      var overflowAbove = root.querySelector('#overflow-above');
      var overflowBelow = root.querySelector('#overflow-below');
      var overflowWithin = root.querySelector('#overflow-within');

      // Mock all bounding rects
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 140
        };
      };
      element.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      overflowAbove.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 110
        };
      };
      overflowWithin.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 120
        };
      };
      overflowBelow.getBoundingClientRect = function () {
        return {
          top: 140,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 150
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', element), function (t) {
        return t.id;
      })).toBe('overflow-within');
    }));
    test('should find target within container larger than overflow container', (0, _utils.testScenario)(scenarios.overflowLargeSubContainer, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      (0, _container10.configureContainer)('inside', {
        enterTo: null
      });
      var overflowContainer = root.querySelector('[data-spotlight-id="overflow-container"]');
      var insideContainer = root.querySelector('[data-spotlight-id="inside"]');
      var element = root.querySelector('#outside-overflow');
      var inLargeContainer = root.querySelector('#in-large-container');
      var belowLargeContainer = root.querySelector('#below-large-container');

      // Mock all bounding rects
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 140
        };
      };
      insideContainer.getBoundingClientRect = function () {
        return {
          top: 100,
          left: 10,
          width: 10,
          height: 50,
          right: 20,
          bottom: 150
        };
      };
      element.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      inLargeContainer.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 120
        };
      };
      belowLargeContainer.getBoundingClientRect = function () {
        return {
          top: 150,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 160
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', element), function (t) {
        return t.id;
      })).toBe('in-large-container');
    }));
    test('should find target out of bounds of overflow container from within container', (0, _utils.testScenario)(scenarios.overflow, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      var overflowContainer = root.querySelector('[data-spotlight-id="overflow-container"]');
      var overflowAbove = root.querySelector('#overflow-above');
      var overflowBelow = root.querySelector('#overflow-below');
      var overflowWithin = root.querySelector('#overflow-within');

      // Mock all bounding rects
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 140
        };
      };
      overflowAbove.getBoundingClientRect = function () {
        return {
          top: 100,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 110
        };
      };
      overflowWithin.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 120
        };
      };
      overflowBelow.getBoundingClientRect = function () {
        return {
          top: 140,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 150
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', overflowWithin), function (t) {
        return t.id;
      })).toBe('overflow-below');
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('up', overflowWithin), function (t) {
        return t.id;
      })).toBe('overflow-above');
    }));
    test('should respect enterTo="default-element" containers', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none',
        enterTo: 'default-element',
        defaultElement: '#bottom-right'
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var afterGrid = root.querySelector('#after-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      afterGrid.getBoundingClientRect = function () {
        return {
          top: 40,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 50
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', beforeGrid), function (t) {
        return t.id;
      })).toBe('bottom-right');
    }));
    test('should respect enterTo="last-focused" containers', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none',
        enterTo: 'last-focused',
        lastFocusedElement: root.querySelector('#bottom-right')
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var afterGrid = root.querySelector('#after-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      afterGrid.getBoundingClientRect = function () {
        return {
          top: 40,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 50
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', beforeGrid), function (t) {
        return t.id;
      })).toBe('bottom-right');
    }));
    test('should follow the leaveFor config when no target is found within the container in the given direction', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none',
        leaveFor: {
          up: '#after-grid'
        }
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var afterGrid = root.querySelector('#after-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      afterGrid.getBoundingClientRect = function () {
        return {
          top: 40,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 50
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('up', topCenter), function (t) {
        return t.id;
      })).toBe('after-grid');
    }));
    test('should not follow the leaveFor config when a target is found within the container in the given direction', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none',
        leaveFor: {
          up: '#after-grid'
        }
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var afterGrid = root.querySelector('#after-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      afterGrid.getBoundingClientRect = function () {
        return {
          top: 40,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 50
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('up', middleCenter), function (t) {
        return t.id;
      })).toBe('top-center');
    }));
    test('should not follow the leaveFor config when the selector does not match an element', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none',
        leaveFor: {
          up: '#does-not-exist'
        }
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var afterGrid = root.querySelector('#after-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      afterGrid.getBoundingClientRect = function () {
        return {
          top: 40,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 50
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('up', topCenter), function (t) {
        return t.id;
      })).toBe('before-grid');
    }));
    test('should ignore empty containers', (0, _utils.testScenario)(scenarios.emptyContainer, function (root) {
      (0, _container10.configureContainer)('empty-container');
      var emptyContainer = root.querySelector('[data-spotlight-id="empty-container"]');
      var above = root.querySelector('#above');
      var below = root.querySelector('#below');

      // Mock all bounding rects
      above.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 10
        };
      };
      emptyContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      below.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 0,
          width: 10,
          height: 10,
          right: 10,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', above), function (t) {
        return t.id;
      })).toBe('below');
    }));
    test('should ignore overlapping empty containers', (0, _utils.testScenario)(scenarios.emptyContainerOverlap, function (root) {
      (0, _container10.configureContainer)('empty-container');
      var emptyContainer = root.querySelector('[data-spotlight-id="empty-container"]');
      var above = root.querySelector('#above');
      var below = root.querySelector('#below');

      // Mock all bounding rects
      above.getBoundingClientRect = function () {
        return {
          top: 5,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 15
        };
      };
      emptyContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      below.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 0,
          width: 10,
          height: 10,
          right: 10,
          bottom: 40
        };
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromElement)('down', above), function (t) {
        return t.id;
      })).toBe('below');
    }));
  });
  describe('#getTargetByDirectionFromPosition', function () {
    test('should find target within container', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid');
      var middleCenter = root.querySelector('#middle-center');
      var topCenter = root.querySelector('#top-center');
      var bottomCenter = root.querySelector('#bottom-center');
      var middleLeft = root.querySelector('#middle-left');
      var middleRight = root.querySelector('#middle-right');
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 0,
          width: 10,
          height: 10
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10
        };
      };
      var centerRect = root.querySelector('#middle-center').getBoundingClientRect();
      var center = {
        x: centerRect.left + centerRect.width / 2,
        y: centerRect.top + centerRect.height / 2
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('up', center, 'grid'), function (t) {
        return t.id;
      })).toBe('top-center');
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', center, 'grid'), function (t) {
        return t.id;
      })).toBe('bottom-center');
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('left', center, 'grid'), function (t) {
        return t.id;
      })).toBe('middle-left');
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('right', center, 'grid'), function (t) {
        return t.id;
      })).toBe('middle-right');
    }));
    test('should keep the same target when at bounds of container with restrict="self-only"', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'self-only'
      });
      var topCenter = root.querySelector('#top-center');
      topCenter.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10
        };
      };
      var rect = root.querySelector('#top-center').getBoundingClientRect();
      var topCenterOfGrid = {
        x: rect.left + rect.width / 2,
        y: rect.top
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('up', topCenterOfGrid, 'grid'), function (t) {
        return t.id;
      })).toBe('top-left');
    }));
    test('should not find a target outside of container when restrict is not set', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none'
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      var rect = topCenter.getBoundingClientRect();
      var topCenterOfGrid = {
        x: rect.left + rect.width / 2,
        y: rect.top
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('up', topCenterOfGrid, 'grid'), function (t) {
        return t.id;
      })).toBe('before-grid');
    }));
    test('should cascade into unrestricted subcontainers', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none'
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      var rect = topCenter.getBoundingClientRect();
      var aboveCenterOfGrid = {
        x: rect.left + rect.width / 2,
        y: rect.top - 1
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', aboveCenterOfGrid, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('top-center');
    }));
    test('should ignore enterTo config of restricted subcontainers', (0, _utils.testScenario)(scenarios.grid, function (root) {
      (0, _container10.configureContainer)('grid', {
        restrict: 'none',
        enterTo: 'default-element',
        defaultElement: '#bottom-right'
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var beforeGrid = root.querySelector('#before-grid');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      beforeGrid.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 30,
          height: 10,
          right: 40,
          bottom: 10
        };
      };
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      var rect = topCenter.getBoundingClientRect();
      var aboveCenterOfGrid = {
        x: rect.left + rect.width / 2,
        y: rect.top - 1
      };
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', aboveCenterOfGrid, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('top-center');
    }));
    test('should find target within container from floating element', (0, _utils.testScenario)(scenarios.overlap, function (root) {
      (0, _container10.configureContainer)('grid', {
        enterTo: 'default-element',
        defaultElement: '#bottom-right'
      });
      var gridContainer = root.querySelector('[data-spotlight-id="grid"]');
      var overlap = root.querySelector('#over-middle-center');
      var topLeft = root.querySelector('#top-left');
      var topCenter = root.querySelector('#top-center');
      var topRight = root.querySelector('#top-right');
      var middleLeft = root.querySelector('#middle-left');
      var middleCenter = root.querySelector('#middle-center');
      var middleRight = root.querySelector('#middle-right');
      var bottomLeft = root.querySelector('#bottom-left');
      var bottomCenter = root.querySelector('#bottom-center');
      var bottomRight = root.querySelector('#bottom-right');

      // Mock all bounding rects
      gridContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 40
        };
      };
      topLeft.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      topCenter.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 20
        };
      };
      topRight.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 20
        };
      };
      middleLeft.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 30
        };
      };
      middleCenter.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 30
        };
      };
      middleRight.getBoundingClientRect = function () {
        return {
          top: 20,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 30
        };
      };
      bottomLeft.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      bottomCenter.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 40
        };
      };
      bottomRight.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 30,
          width: 10,
          height: 10,
          right: 40,
          bottom: 40
        };
      };
      // Overlap is floating over middle-center
      overlap.getBoundingClientRect = function () {
        return {
          top: 22,
          left: 25,
          width: 1,
          height: 1,
          right: 26,
          bottom: 23
        };
      };
      var _overlap$getBoundingC = overlap.getBoundingClientRect(),
        x = _overlap$getBoundingC.left,
        y = _overlap$getBoundingC.top;
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x,
        y: y
      }, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('middle-center');
    }));
    test('should ignore targets outside the bounds of an overflow container', (0, _utils.testScenario)(scenarios.overflow, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      var overflowContainer = root.querySelector('[data-spotlight-id="overflow-container"]');
      var element = root.querySelector('#outside-overflow');
      var overflowAbove = root.querySelector('#overflow-above');
      var overflowBelow = root.querySelector('#overflow-below');
      var overflowWithin = root.querySelector('#overflow-within');

      // Mock all bounding rects
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 50,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 80
        };
      };
      element.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      overflowAbove.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 40
        };
      };
      overflowWithin.getBoundingClientRect = function () {
        return {
          top: 55,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 65
        };
      };
      overflowBelow.getBoundingClientRect = function () {
        return {
          top: 85,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 95
        };
      };
      var _element$getBoundingC = element.getBoundingClientRect(),
        x = _element$getBoundingC.left,
        y = _element$getBoundingC.top;
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x + 15,
        y: y + 35
      }, _container10.rootContainerId),
      // position is inside #outside-overflow
      function (t) {
        return t.id;
      })).toBe('overflow-within');
    }));
    test('should find target within container larger than overflow container', (0, _utils.testScenario)(scenarios.overflowLargeSubContainer, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      (0, _container10.configureContainer)('inside', {
        enterTo: null
      });
      var overflowContainer = root.querySelector('[data-spotlight-id="overflow-container"]');
      var insideContainer = root.querySelector('[data-spotlight-id="inside"]');
      var element = root.querySelector('#outside-overflow');
      var inLargeContainer = root.querySelector('#in-large-container');
      var belowLargeContainer = root.querySelector('#below-large-container');

      // Mock all bounding rects
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 50,
          left: 10,
          width: 40,
          height: 40,
          right: 50,
          bottom: 90
        };
      };
      insideContainer.getBoundingClientRect = function () {
        return {
          top: 40,
          left: 15,
          width: 30,
          height: 60,
          right: 45,
          bottom: 100
        };
      };
      element.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      inLargeContainer.getBoundingClientRect = function () {
        return {
          top: 60,
          left: 20,
          width: 10,
          height: 10,
          right: 30,
          bottom: 70
        };
      };
      belowLargeContainer.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 120
        };
      };
      var _element$getBoundingC2 = element.getBoundingClientRect(),
        x = _element$getBoundingC2.left,
        y = _element$getBoundingC2.top;
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x + 5,
        y: y + 5
      }, _container10.rootContainerId),
      // position is inside #outside-overflow
      function (t) {
        return t.id;
      })).toBe('in-large-container');
    }));
    test('should find target out of bounds of overflow container from within container', (0, _utils.testScenario)(scenarios.overflow, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      var overflowContainer = root.querySelector('[data-spotlight-id="overflow-container"]');
      var overflowAbove = root.querySelector('#overflow-above');
      var overflowBelow = root.querySelector('#overflow-below');
      var overflowWithin = root.querySelector('#overflow-within');

      // Mock all bounding rects
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 30,
          height: 30,
          right: 40,
          bottom: 140
        };
      };
      overflowAbove.getBoundingClientRect = function () {
        return {
          top: 100,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 110
        };
      };
      overflowWithin.getBoundingClientRect = function () {
        return {
          top: 110,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 120
        };
      };
      overflowBelow.getBoundingClientRect = function () {
        return {
          top: 140,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 150
        };
      };
      var _overflowWithin$getBo = overflowWithin.getBoundingClientRect(),
        left = _overflowWithin$getBo.left,
        width = _overflowWithin$getBo.width,
        top = _overflowWithin$getBo.top,
        height = _overflowWithin$getBo.height;
      var x = left + width / 2;
      var y = top + height / 2;
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x,
        y: y + 1
      }, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('overflow-below');
      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('up', {
        x: x,
        y: y - 1
      }, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('overflow-above');
    }));
    test('should ignore empty containers', (0, _utils.testScenario)(scenarios.emptyContainer, function (root) {
      (0, _container10.configureContainer)('empty-container');
      var emptyContainer = root.querySelector('[data-spotlight-id="empty-container"]');
      var above = root.querySelector('#above');
      var below = root.querySelector('#below');

      // Mock all bounding rects
      above.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 10
        };
      };
      emptyContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      below.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 0,
          width: 10,
          height: 10,
          right: 10,
          bottom: 40
        };
      };
      var _above$getBoundingCli = above.getBoundingClientRect(),
        left = _above$getBoundingCli.left,
        width = _above$getBoundingCli.width,
        top = _above$getBoundingCli.top,
        height = _above$getBoundingCli.height;
      var x = left + width / 2;
      var y = top + height - 1; // just inside the bottom of 'above'

      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x,
        y: y
      }, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('below');
    }));
    test('should ignore overlapping empty containers', (0, _utils.testScenario)(scenarios.emptyContainer, function (root) {
      (0, _container10.configureContainer)('empty-container');
      var emptyContainer = root.querySelector('[data-spotlight-id="empty-container"]');
      var above = root.querySelector('#above');
      var below = root.querySelector('#below');

      // Mock all bounding rects
      above.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 10
        };
      };
      emptyContainer.getBoundingClientRect = function () {
        return {
          top: 10,
          left: 10,
          width: 10,
          height: 10,
          right: 20,
          bottom: 20
        };
      };
      below.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 0,
          width: 10,
          height: 10,
          right: 10,
          bottom: 40
        };
      };
      var _above$getBoundingCli2 = above.getBoundingClientRect(),
        left = _above$getBoundingCli2.left,
        width = _above$getBoundingCli2.width,
        top = _above$getBoundingCli2.top,
        height = _above$getBoundingCli2.height;
      var x = left + width / 2;
      var y = top + height + 1; // just inside the empty container

      expect(safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x,
        y: y
      }, _container10.rootContainerId), function (t) {
        return t.id;
      })).toBe('below');
    }));
  });
  describe('#directional visibility across containers', function () {
    test('should skip invisible target in another container and select a visible one', (0, _utils.testScenario)(scenarios.overflow, function (root) {
      (0, _container10.configureContainer)('overflow-container', {
        overflow: true
      });
      var overflowContainer = root.querySelector("[".concat(_container10.containerAttribute, "='overflow-container']"));
      var outside = root.querySelector('#outside-overflow');
      var above = root.querySelector('#overflow-above');
      var within = root.querySelector('#overflow-within');
      var below = root.querySelector('#overflow-below');
      overflowContainer.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 0,
          bottom: 30,
          right: 30,
          width: 30,
          height: 30
        };
      };
      outside.getBoundingClientRect = function () {
        return {
          top: -20,
          left: 0,
          bottom: -10,
          right: 10,
          width: 10,
          height: 10
        };
      };
      above.getBoundingClientRect = function () {
        return {
          top: -10,
          left: 0,
          bottom: -5,
          right: 10,
          width: 10,
          height: 10
        };
      };
      within.getBoundingClientRect = function () {
        return {
          top: 0,
          left: 0,
          bottom: 10,
          right: 10,
          width: 10,
          height: 10
        };
      };
      below.getBoundingClientRect = function () {
        return {
          top: 30,
          left: 0,
          bottom: 40,
          right: 10,
          width: 10,
          height: 10
        };
      };
      var _outside$getBoundingC = outside.getBoundingClientRect(),
        left = _outside$getBoundingC.left,
        right = _outside$getBoundingC.right,
        top = _outside$getBoundingC.top,
        bottom = _outside$getBoundingC.bottom;
      var x = (left + right) / 2;
      var y = (top + bottom) / 2;
      var expected = 'overflow-within';
      var actualFromPosition = safeTarget((0, _target.getTargetByDirectionFromPosition)('down', {
        x: x,
        y: y
      }, _container10.rootContainerId), function (t) {
        return t.id;
      });
      var actualFromElement = safeTarget((0, _target.getTargetByDirectionFromElement)('down', outside), function (t) {
        return t.id;
      });
      expect(actualFromPosition).toBe(expected);
      expect(actualFromElement).toBe(expected);
    }));
  });
});