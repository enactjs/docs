"use strict";

require("@testing-library/jest-dom");
var _react = require("@testing-library/react");
var _useScroll = require("../useScroll");
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
// Mock all dependencies at the top level BEFORE importing useScroll
var mockRiScale;
var mockPlatform;
jest.mock('../../resolution', function () {
  return {
    __esModule: true,
    "default": {
      get scale() {
        return mockRiScale || function (val) {
          return val;
        };
      }
    }
  };
});
jest.mock('@enact/core/platform', function () {
  return {
    __esModule: true,
    get platform() {
      return mockPlatform || {
        chrome: 120
      };
    }
  };
});
global.ResizeObserver = /*#__PURE__*/function () {
  function ResizeObserver() {
    _classCallCheck(this, ResizeObserver);
  }
  return _createClass(ResizeObserver, [{
    key: "observe",
    value: function observe() {}
  }, {
    key: "unobserve",
    value: function unobserve() {}
  }, {
    key: "disconnect",
    value: function disconnect() {}
  }]);
}();

// Import AFTER all mocks

// Helper function to create complete mock refs
function createMockRefs() {
  return {
    scrollContentHandle: {
      current: {
        getScrollBounds: jest.fn(function () {
          return {
            clientWidth: 1920,
            clientHeight: 1080,
            scrollWidth: 1000,
            scrollHeight: 800,
            maxTop: 200,
            maxLeft: 200
          };
        }),
        getMoreInfo: jest.fn(function () {
          return {};
        }),
        hasDataSizeChanged: false,
        syncClientSize: jest.fn(function () {
          return false;
        }),
        getRtlPositionX: jest.fn(function (x) {
          return x;
        }),
        calculateMetrics: jest.fn(),
        props: {}
      }
    },
    scrollContainerRef: {
      current: document.createElement('div')
    },
    scrollContentRef: {
      current: document.createElement('div')
    },
    horizontalScrollbarHandle: {
      current: {
        update: jest.fn(),
        getContainerRef: jest.fn(function () {
          return document.createElement('div');
        }),
        startHidingScrollbarTrack: jest.fn()
      }
    },
    verticalScrollbarHandle: {
      current: {
        update: jest.fn(),
        getContainerRef: jest.fn(function () {
          return document.createElement('div');
        }),
        startHidingScrollbarTrack: jest.fn()
      }
    }
  };
}
describe('useScroll', function () {
  describe('riRatio itemSize scaling on window resize', function () {
    beforeEach(function () {
      jest.useFakeTimers();
      mockPlatform = {
        chrome: 132
      };
    });
    afterEach(function () {
      jest.runOnlyPendingTimers();
      jest.useRealTimers();
    });
    test('should scale itemSize when ri.scale(1) changes from 1 to 2', function () {
      mockRiScale = jest.fn(function (val) {
        return val;
      });
      var itemSize = {
        minWidth: 100,
        minHeight: 50
      };
      var mocks = createMockRefs();
      var props = _objectSpread(_objectSpread({
        itemRenderer: jest.fn(),
        itemSize: itemSize,
        direction: 'vertical',
        scrollMode: 'translate'
      }, mocks), {}, {
        assignProperties: jest.fn(),
        horizontalScrollbar: 'auto',
        verticalScrollbar: 'auto'
      });
      (0, _react.renderHook)(function () {
        return (0, _useScroll.useScrollBase)(props);
      });
      mockRiScale = jest.fn(function () {
        return 2;
      });

      // eslint-disable-next-line testing-library/no-unnecessary-act
      (0, _react.act)(function () {
        (0, _react.fireEvent)(window, new Event('resize'));
        jest.runAllTimers();
      });
      expect(itemSize.minWidth).toBe(200);
      expect(itemSize.minHeight).toBe(100);
    });
    test('should NOT scale when ri.scale(1) remains unchanged', function () {
      mockRiScale = jest.fn(function (val) {
        return val;
      });
      var itemSize = {
        minWidth: 100,
        minHeight: 50
      };
      var mocks = createMockRefs();
      var props = _objectSpread(_objectSpread({
        itemRenderer: jest.fn(),
        itemSize: itemSize,
        direction: 'vertical',
        scrollMode: 'translate'
      }, mocks), {}, {
        assignProperties: jest.fn(),
        horizontalScrollbar: 'auto',
        verticalScrollbar: 'auto'
      });
      (0, _react.renderHook)(function () {
        return (0, _useScroll.useScrollBase)(props);
      });

      // eslint-disable-next-line testing-library/no-unnecessary-act
      (0, _react.act)(function () {
        (0, _react.fireEvent)(window, new Event('resize'));
        jest.runAllTimers();
      });
      expect(itemSize.minWidth).toBe(100);
      expect(itemSize.minHeight).toBe(50);
    });
    test('should NOT scale when minWidth is missing', function () {
      mockRiScale = jest.fn(function (val) {
        return val;
      });
      var itemSize = {
        minHeight: 50
      };
      var mocks = createMockRefs();
      var props = _objectSpread(_objectSpread({
        itemRenderer: jest.fn(),
        itemSize: itemSize,
        direction: 'vertical',
        scrollMode: 'translate'
      }, mocks), {}, {
        assignProperties: jest.fn(),
        horizontalScrollbar: 'auto',
        verticalScrollbar: 'auto'
      });
      (0, _react.renderHook)(function () {
        return (0, _useScroll.useScrollBase)(props);
      });
      mockRiScale = jest.fn(function () {
        return 2;
      });

      // eslint-disable-next-line testing-library/no-unnecessary-act
      (0, _react.act)(function () {
        (0, _react.fireEvent)(window, new Event('resize'));
        jest.runAllTimers();
      });
      expect(itemSize.minHeight).toBe(50);
      expect(itemSize.minWidth).toBeUndefined();
    });
    test('should NOT scale when minHeight is missing', function () {
      mockRiScale = jest.fn(function (val) {
        return val;
      });
      var itemSize = {
        minWidth: 100
      };
      var mocks = createMockRefs();
      var props = _objectSpread(_objectSpread({
        itemRenderer: jest.fn(),
        itemSize: itemSize,
        direction: 'vertical',
        scrollMode: 'translate'
      }, mocks), {}, {
        assignProperties: jest.fn(),
        horizontalScrollbar: 'auto',
        verticalScrollbar: 'auto'
      });
      (0, _react.renderHook)(function () {
        return (0, _useScroll.useScrollBase)(props);
      });
      mockRiScale = jest.fn(function () {
        return 2;
      });

      // eslint-disable-next-line testing-library/no-unnecessary-act
      (0, _react.act)(function () {
        (0, _react.fireEvent)(window, new Event('resize'));
        jest.runAllTimers();
      });
      expect(itemSize.minWidth).toBe(100);
      expect(itemSize.minHeight).toBeUndefined();
    });
  });
  describe('Rounding target scroll position', function () {
    beforeEach(function () {
      jest.resetModules(); // important! clears cached modules
    });
    test('should round target position upwards when target is bigger than current position', function () {
      mockPlatform = {
        chrome: 132
      };
      var _require = require('../useScroll'),
        roundTarget = _require.roundTarget;
      var currentPosition = {
        scrollPos: {
          left: 100,
          top: 100
        }
      };
      var _roundTarget = roundTarget(currentPosition, 120.5, 120.4),
        roundedTargetX = _roundTarget.roundedTargetX,
        roundedTargetY = _roundTarget.roundedTargetY;
      expect(roundedTargetX).toEqual(121);
      expect(roundedTargetY).toEqual(121);
    });
    test('should round target position downwards when target is bi than current position', function () {
      mockPlatform = {
        chrome: 132
      };
      var _require2 = require('../useScroll'),
        roundTarget = _require2.roundTarget;
      var currentPosition = {
        scrollPos: {
          left: 100,
          top: 100
        }
      };
      var _roundTarget2 = roundTarget(currentPosition, 90.4, 80.8),
        roundedTargetX = _roundTarget2.roundedTargetX,
        roundedTargetY = _roundTarget2.roundedTargetY;
      expect(roundedTargetX).toEqual(90);
      expect(roundedTargetY).toEqual(80);
    });
    test('should not round target position when chrome <= 120', function () {
      mockPlatform = {
        chrome: 119
      };
      var _require3 = require('../useScroll'),
        roundTarget = _require3.roundTarget;
      var currentPosition = {
        scrollPos: {
          left: 100,
          top: 100
        }
      };
      var _roundTarget3 = roundTarget(currentPosition, 90.4, 80.8),
        roundedTargetX = _roundTarget3.roundedTargetX,
        roundedTargetY = _roundTarget3.roundedTargetY;
      expect(roundedTargetX).toEqual(90.4);
      expect(roundedTargetY).toEqual(80.8);
    });
  });
});