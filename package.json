{
  "name": "my-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "parse": "npm run parse-docs --",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "fast-deploy": "npm run make-runner -- --fast && npm run build",
    "ci-deploy": "npm run parse-docs && npm run json-to-mdx && npm run make-runner && npm run build",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "parse-docs": "node scripts/DocParser.js",
    "json-to-mdx": "node scripts/jsonParser.js",
    "make-runner": "node scripts/make-runner.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "@docusaurus/core": "3.9.2",
    "@docusaurus/preset-classic": "3.9.2",
    "@enact/core": "^5.4.0",
    "@enact/docs-utils": "^0.4.16",
    "@enact/ui": "^5.4.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.2",
    "@docusaurus/types": "3.9.2",
    "eslint": "^10.1.0",
    "eslint-config-enact": "^5.0.3"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
