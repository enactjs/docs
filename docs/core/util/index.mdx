---
title: "core/util"
description: "API documentation for core/util"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# core/util

A collection of utility methods.


## Exports

This module exports the following:

- `cap`
- `clamp`
- `coerceArray`
- `coerceFunction`
- `extractAriaProps`
- `isRenderable`
- `Job`
- `memoize`
- `mergeClassNameMaps`
- `perfNow`
- `mapAndFilterChildren`
- `shallowEqual`


## API Reference


## Functions

### cap

<span className="badge badge--secondary">function</span> 

Capitalizes a given string (not locale-aware).


#### Parameters

<Tabs>
  <TabItem value="str" label="str">
    **Type:** `String`

    The string to capitalize.
  </TabItem>
</Tabs>


#### Returns

**Type:** `String`

The capitalized string.


---

### clamp

<span className="badge badge--secondary">function</span> 

Limits `value` to be between `min` and `max`.

If `min` is greater than `max`, `min` is returned.


#### Parameters

<Tabs>
  <TabItem value="min" label="min">
    **Type:** `Number`

    The minimum value of the range
  </TabItem>
  <TabItem value="max" label="max">
    **Type:** `Number`

    The maximum value of the range
  </TabItem>
  <TabItem value="value" label="value">
    **Type:** `Number`

    The value that must be within the range
  </TabItem>
</Tabs>


#### Returns

**Type:** `Number`

The clamped value


---

### coerceFunction

<span className="badge badge--secondary">function</span> 

If `arg` is a function, return it. Otherwise returns a function that returns `arg`.

Example:

```
const returnsZero = coerceFunction(0);
const returnsArg = coerceFunction(() => 0);
```


#### Parameters

<Tabs>
  <TabItem value="arg" label="arg">
    **Type:** `*`

    Function or value
  </TabItem>
</Tabs>


#### Returns

**Type:** `Function`

Either `arg` if `arg` is a function, or a function that returns `arg`


---

### coerceArray

<span className="badge badge--secondary">function</span> 

If `arg` is array-like, return it. Otherwise returns a single element array containing `arg`.

Example:

```
const returnsArray = coerceArray(0); // [0]
const returnsArg = coerceArray([0]); // [0]
const returnsObjArg = coerceArray({0: 'zeroth', length: 1});
```


#### Parameters

<Tabs>
  <TabItem value="array" label="array">
    **Type:** `*`

    Array or value
  </TabItem>
</Tabs>


#### Returns

**Type:** `Array`

Either `array` or `[array]`


### See Also

- [http://ramdajs.com/docs/#isArrayLike](http://ramdajs.com/docs/#isArrayLike)




---

### isRenderable

<span className="badge badge--secondary">function</span> 

Loosely determines if `tag` is a renderable component (either a string or a function).


#### Parameters

<Tabs>
  <TabItem value="tag" label="tag">
    **Type:** `*`

    Component to test
  </TabItem>
</Tabs>


#### Returns

**Type:** `Boolean`

`true` if `tag` is either a string or a function


---

### extractAriaProps

<span className="badge badge--secondary">function</span> 

Removes ARIA-related props from `props` and returns them in a new object.

Specifically, it removes the `role` prop and any prop prefixed with `aria-`. This is useful when
redirecting ARIA-related props from a non-focusable root element to a focusable child element.


#### Parameters

<Tabs>
  <TabItem value="props" label="props">
    **Type:** `Object`

    Props object
  </TabItem>
</Tabs>


#### Returns

**Type:** `Object`

ARIA-related props


---

### perfNow

<span className="badge badge--secondary">function</span> 

Gets the current timestamp of either `window.performance.now` or `Date.now`


#### Returns

**Type:** `Number`

The timestamp from `window.performance.now` or `Date.now`


---

### mergeClassNameMaps

<span className="badge badge--secondary">function</span> 

Merges two class name maps into one.

The resulting map will only contain the class names defined in the `baseMap` and will be appended
with the value from `additiveMap` if it exists. Further, `allowedClassNames` may optionally limit
which keys will be merged from `additiveMap` into `baseMap`.

Example:

```
// merges all matching class names from additiveMap1 with baseMap1
const newMap1 = mergeClassNameMaps(baseMap1, additiveMap1);

// merge only 'a' and 'b' class names from additiveMap2 with baseMap2
const newMap2 = mergeClassNameMaps(baseMap2, additiveMap2, ['a', 'b']);
```


#### Parameters

<Tabs>
  <TabItem value="baseMap" label="baseMap">
    **Type:** `Object`

    The source mapping of logical class name to physical
class name
  </TabItem>
  <TabItem value="additiveMap" label="additiveMap">
    **Type:** `Object`

    Mapping of logical to physical class names which are
concatenated with `baseMap` where the logical names match
  </TabItem>
  <TabItem value="allowedClassNames" label="allowedClassNames">
    **Type:** `Array<String>?` *(optional)*

    Array of logical class names that can be augmented. When
set, the logical class name must exist in `baseMap`,
`additiveMap`, and this array to be concatenated.
  </TabItem>
</Tabs>


#### Returns

**Type:** `Object`

The merged class name map.


---

### memoize

<span className="badge badge--secondary">function</span> 

Creates a function that memoizes the result of `fn`.

Note that this function is a naive implementation and only checks the first argument for
memoization.


#### Parameters

<Tabs>
  <TabItem value="fn" label="fn">
    **Type:** `Function`

    The function to have its output memoized.
  </TabItem>
</Tabs>


#### Returns

**Type:** `Function`

The new memoized function.


---

### mapAndFilterChildren

<span className="badge badge--secondary">function</span> 

Maps over the `children`, discarding any `null` children before and after calling the callback.

A replacement for `React.Children.map`.


#### Parameters

<Tabs>
  <TabItem value="children" label="children">
    **Type:** `*`

    Children to map over
  </TabItem>
  <TabItem value="callback" label="callback">
    **Type:** `Function`

    Function to apply to each child. Will not be called if the child is
`null`. If `callback` returns `null`, the child will be removed from
the result. If `null` is returned, the item will not be included in
the final output, regardless of the filter function.
  </TabItem>
  <TabItem value="filter" label="filter">
    **Type:** `Function?` *(optional)*

    Filter function applied after mapping.
  </TabItem>
</Tabs>


#### Returns

**Type:** `*`

The processed children or the value of `children` if not an array.


### See Also

- [https://react.dev/reference/react/Children#children-map](https://react.dev/reference/react/Children#children-map)




---

### setDefaultProps

<span className="badge badge--secondary">function</span> 

Sets props that are missing or `undefined` to default values


#### Parameters

<Tabs>
  <TabItem value="props" label="props">
    **Type:** `Object`

    Props object
  </TabItem>
  <TabItem value="defaultProps" label="defaultProps">
    **Type:** `Object`

    Default value object
  </TabItem>
</Tabs>


#### Returns

**Type:** `Object`

Props with default values


---

### shallowEqual

<span className="badge badge--secondary">function</span> 

Performs shallow comparison for given objects.


#### Parameters

<Tabs>
  <TabItem value="a" label="a">
    **Type:** `Object`

    An object to compare.
  </TabItem>
  <TabItem value="b" label="b">
    **Type:** `Object`

    An object to compare.
  </TabItem>
</Tabs>


#### Returns

**Type:** `Boolean`

`true` if the values of all keys are strictly equal.


---


### Classes

#### Job

<span className="badge badge--secondary">class</span> 

Provides a convenient way to manage timed execution of functions.


---


#### Instance Members

##### start

<span className="badge badge--secondary">function</span> 

Starts the job.


#### Parameters

<Tabs>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### startAfter

<span className="badge badge--secondary">function</span> 

Starts the job in `timeout` milliseconds


#### Parameters

<Tabs>
  <TabItem value="timeout" label="timeout">
    **Type:** `Number`

    The number of milliseconds to wait before starting the job.
This supersedes the timeout set at construction.
  </TabItem>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### stop

<span className="badge badge--secondary">function</span> 

Stops the job.


#### Returns

**Type:** `any`


---

##### throttle

<span className="badge badge--secondary">function</span> 

Executes the job immediately, then prevents any other calls to `throttle()` from running
until the `timeout` configured at construction passes.


#### Parameters

<Tabs>
  <TabItem value="args" label="args">
    **Type:** `...*`

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### throttleUntil

<span className="badge badge--secondary">function</span> 

Executes the job immediately, then prevents any other calls to `throttle()` from running for
`timeout` milliseconds.


#### Parameters

<Tabs>
  <TabItem value="timeout" label="timeout">
    **Type:** `Number`

    The number of milliseconds to wait before allowing the job to
be ran again. This supersedes the timeout set at construction.
  </TabItem>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### idle

<span className="badge badge--secondary">function</span> 

Executes job when the CPU is idle.


#### Parameters

<Tabs>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### idleUntil

<span className="badge badge--secondary">function</span> 

Executes job when the CPU is idle, or when the timeout is reached, whichever occurs first.


#### Parameters

<Tabs>
  <TabItem value="timeout" label="timeout">
    **Type:** `Number`

    The number of milliseconds to wait before executing the
job. This guarantees that the job is run, if a positive value
is specified.
  </TabItem>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### startRaf

<span className="badge badge--secondary">function</span> 

Executes job before the next repaint.


#### Parameters

<Tabs>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### startRafAfter

<span className="badge badge--secondary">function</span> 

Executes job before the next repaint after a given amount of timeout.


#### Parameters

<Tabs>
  <TabItem value="timeout" label="timeout">
    **Type:** `Number`

    The number of milliseconds to wait before running `requestAnimationFrame`.
  </TabItem>
  <TabItem value="args" label="args">
    **Type:** `...*?` *(optional)*

    Any args passed are forwarded to the callback
  </TabItem>
</Tabs>


#### Returns

**Type:** `any`


---

##### promise

<span className="badge badge--secondary">function</span> 

Starts the job when `promise` resolves.

The job execution is tied to the resolution of the last `Promise` passed. Like other methods
on `Job`, calling `promise()` again with a new `Promise` will block execution of the job
until that new `Promise` resolves. Any previous `Promise`s will still resolve normally but
will not trigger job execution.

Unlike other methods on `Job`, `promise()` returns a `Promise` which is the result of calling
`then()` on the passed `promise`. That `Promise` resolves with either the result of job
execution or `undefined` if `Promise` was superseded by a subsequent `Promise` passed as
described above.


#### Parameters

<Tabs>
  <TabItem value="promise" label="promise">
    **Type:** `Promise`

    The promise that must resolve before the job is executed
  </TabItem>
</Tabs>


#### Returns

**Type:** `Promise`


---

