---
title: "ui/Icon"
description: "API documentation for ui/Icon"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# ui/Icon

An unstyled icon component to be customized by a theme or application.


## Exports

This module exports the following:

- `Icon`
- `IconDecorator`


## API Reference


### Classes

#### IconBase

<span className="badge badge--secondary">class</span> 

A basic icon component structure without any behaviors applied to it.


---


#### Instance Members

##### children

The icon content.

May be specified as either:


- A string that represents an icon from the [iconList](#ui-icon.iconbase.iconlist),
- An HTML entity string, Unicode reference or hex value (in the form '0x...'),
- A URL specifying path to an icon image, or
- An object representing a resolution independent resource (See [ui/resolution](#ui-resolution)).


---

##### componentRef

Called with a reference to the root component.

When using [ui/Icon.Icon](#ui-icon.icon), the `ref` prop is forwarded to this component
as `componentRef`.


---

##### css

Customizes the component by mapping the supplied collection of CSS class names to the
corresponding internal elements and states of this component.

The following classes are supported:


- `icon` - The root component class
- `dingbat` - Applied when the value of [icon](#ui-icon.iconbase.icon) is not
found in [iconList](#ui-icon.iconbase.iconlist)
- `large` - Applied when `size` prop is `'large'`
- `pressed` - Applied when `pressed` prop is `true`
- `small` - Applied when `size` prop is `'small'`


---

##### flip

Flip the icon horizontally, vertically or both.


---

##### iconList

The full list (hash) of supported icons.

The keys of this hash are the unique names of each icon. The values are the unicode
characters to insert in the icon. These will typically map to glyphs in your icon-font.
The format of the keys can be character, glyph, or entity reference that correctly
renders in a React + JSX string.


---

##### pressed

Applies the `pressed` CSS class.


---

##### size

The size of the button.

Applies the CSS class which can be customized by
[theming](#-docs-developer-guide-theming-).


---

#### Icon

<span className="badge badge--secondary">class</span> 

An Icon component.


---


## Constants

#### IconDecorator

<span className="badge badge--secondary">constant</span> 

A higher-order component that adds behavior to [Icon](#ui-icon.iconbase).


---

